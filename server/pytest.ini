[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# conftest.py does `import main`, which only resolves if the server/ directory
# is on sys.path. Without this, `pytest` fails at collection unless callers
# remember to prefix every run with PYTHONPATH=.
pythonpath = .