name: ayuvo
description: "Ayuvo — your personal digital health record"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# Read more about Windows versioning at
# https://developer.microsoft.com/en-us/windows/apps/publish/versioning
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  dio: ^5.11.0
  go_router: ^17.4.0
  flutter_secure_storage: ^10.3.1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  fl_chart: ^1.2.0
  file_picker: ^12.0.0
  image_picker: ^1.2.3
  printing: ^5.15.0
  pdf: ^3.13.0
  path_provider: ^2.1.6
  # Firebase Cloud Messaging for reliable push reminders.
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.0
  # Browser APIs (localStorage, Notification, PushManager, ServiceWorker) for
  # the web build. Present transitively already; declared so the web-only files
  # that import it can lint cleanly.
  web: ^1.1.1
  # Pinned by flutter_localizations, which depends on exactly 0.20.2. Raising
  # this constraint makes the SDK package unresolvable — see the phase 6 group
  # below.
  intl: ^0.20.2
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.2
  share_plus: ^13.3.0
  # Phase 6. The four browser-only pieces the web app used, replaced:
  # Leaflet → flutter_map, Web Push → flutter_local_notifications (scheduled
  # locally, so the frozen backend needs nothing), Web Speech was dropped with
  # the AI assistant feature, and `front/lib/i18n.tsx` →
  # flutter_localizations + ARB.
  flutter_localizations:
    sdk: flutter
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  geolocator: ^14.0.3
  flutter_local_notifications: ^22.2.0
  # A reminder fires at a wall-clock time, so it has to be scheduled in a named
  # zone rather than as an offset — `timezone` holds the database and
  # `flutter_timezone` asks the OS which entry in it the user is standing in.
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Drives the real app on a real device — see integration_test/.
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information on deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  freezed: ^3.2.5
  json_serializable: ^6.14.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  uses-material-design: true

  fonts:
    - family: Figtree
      fonts:
        - asset: assets/fonts/Figtree-Regular.ttf
        - asset: assets/fonts/Figtree-Medium.ttf
          weight: 500
        - asset: assets/fonts/Figtree-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Figtree-Bold.ttf
          weight: 700
    - family: Noto Sans
      fonts:
        - asset: assets/fonts/NotoSans-Regular.ttf
        - asset: assets/fonts/NotoSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/NotoSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/NotoSans-Bold.ttf
          weight: 700
    - family: Noto Sans Devanagari
      fonts:
        - asset: assets/fonts/NotoSansDevanagari-Regular.ttf

  assets:
    - assets/fonts/

  # Builds AppLocalizations from lib/l10n/*.arb — see l10n.yaml.
  generate: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies, see
  # https://flutter.dev/to/font-from-package