<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The app is a client for a remote health record; without this a release
         build has no network at all. The debug source set adds it too, for the
         Flutter tool's own connection. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Dose reminders. Runtime-requested on Android 13+, and only at the
         moment the user turns the reminders switch on — never at launch.
         SCHEDULE_EXACT_ALARM is deliberately absent: reminders are scheduled
         with inexactAllowWhileIdle, which needs no permission and is accurate
         enough for "take this at eight". -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Without this the alarms are lost on reboot and stay lost until the app
         is next opened. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Nearby care. Coarse is enough for "hospitals within 4 km" and is the
         weaker of the two, so it is what the app actually asks for; fine is
         declared because geolocator's medium accuracy may use it where it is
         already granted. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Voice input in the assistant. Optional: the mic button is hidden when
         no recogniser is available, and the screen works by typing. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Fingerprint / Face unlock as a shortcut past the password screen.
         USE_BIOMETRIC is the API 28+ permission; local_auth degrades to the
         deprecated USE_FINGERPRINT below that, which is why both are listed. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <application
        android:label="Ayuvo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications. The first fires a scheduled reminder;
             the second re-registers every pending one after a reboot, which
             Android otherwise drops on the floor. Neither is exported: only
             the system may start them. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ hides installed apps unless they are declared here, and
             url_launcher's canLaunchUrl answers false for anything it cannot
             see. Without the first two, "Directions" and the emergency
             contact's phone number both silently do nothing on a modern
             phone — the failure this package visibility rule is famous for. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- speech_to_text, for the assistant's mic button. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
