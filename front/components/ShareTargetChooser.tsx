'use client';

/**
 * Removed: share links now always open on the website.
 * The "Open in App" banner caused confusion when recipients were redirected
 * to a separate origin instead of staying on the web reader.
 */
export default function ShareTargetChooser() {
  return null;
}
